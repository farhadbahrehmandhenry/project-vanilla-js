// import * as utils from './export.js';
import { firstText, secondText, thirdText as asText } from './export.js';

// see the result in the browser by running the index.html
console.log(firstText);
console.log(secondText);
console.log(asText);
